# Sistema Sussy - Dependencias
# Instalar con: pip install -r requirements.txt

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0

# Machine Learning / YOLO
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# ONNX Runtime (para inferencia sin CUDA)
onnxruntime>=1.16.0
onnxruntime-directml>=1.16.0  # Para DirectML (Windows GPU sin CUDA)

# Utilidades
PyYAML>=6.0
Pillow>=9.0.0

# Opcional: Para entrenamiento en la nube
# ray[tune]>=2.0.0  # Descomentar si se usa Ray Tune
# wandb>=0.15.0     # Descomentar si se usa Weights & Biases

# Desarrollo (opcional, instalar por separado)
# pytest>=7.0.0
# black>=23.0.0
# ruff>=0.1.0
